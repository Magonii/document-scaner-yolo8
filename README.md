# document-scaner-yolo8
“PDF Object Detection tool using YOLO and Gradio. Automatically finds signatures, stamps, and QR codes in PDF files, annotates them directly on the pages, and generates structured JSON outputs. Supports single PDFs and ZIP batches with fast batched inference and clean UI.”
